from __future__ import annotations
from typing import List, Dict


def estimate_pickup_beats(tempo_map: List[Dict[str, float]], ts_num: int) -> float:
    """Return a heuristic pickup length in beats. Placeholder: returns 0.0.

    Future: analyze early lyrics or leading silence against first downbeat.
    """
    return 0.0
