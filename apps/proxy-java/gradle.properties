# Java home is intentionally not set here to keep this cross-platform.
# Gradle toolchains (configured in build.gradle) or your system JAVA_HOME will be used.

# Enable Gradle daemon for faster builds
org.gradle.daemon=true

# Use parallel execution for faster builds
org.gradle.parallel=true